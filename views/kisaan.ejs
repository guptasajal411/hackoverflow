<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/kisaan.css">
    <title>Document</title>
</head>
<body>
    <div class="centerContainer">
        <h1>Welcome, <%= kisaan.name %></h1>
        <h3>Your yeild: </h3>
        <div class="container">
            <div class="row">
                <% kisaan.yeild.forEach(function(crop){ %>
                    <div class="col-3 card mx-2">
                        <div class="card-body">
                            <h5><%= crop.cropName %></h5>
                            <p><%= crop.quantity %> kgs</p>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
            <h3 class="mt-4">Add new yeild: </h3>
            <form action="/kisaan" method="POST">
                <div class="input-group my-3">
                    <span class="input-group-text" id="basic-addon1">Crop name</span>
                    <input name="cropName" required type="text" class="form-control me-2" placeholder="Crop name" aria-label="Crop name" aria-describedby="basic-addon1">
                    <span class="input-group-text ms-2" id="basic-addon1">Quantity</span>
                    <input name="quantity" required type="number" class="form-control" placeholder="Quantity" aria-label="Quantity" aria-describedby="basic-addon1">
                </div>
                <button class="btn btn-primary"name="email" value="<%= kisaan.email %>" type="submit">Submit</button>
            </form>
        <h1 class="my-3">Cold storage status</h1>
        <div class="container">
            <div class="row coldStorageRow">
                <% foundStorages.forEach(function(storage){ %>
                    <div class="col-3 card mx-2">
                        <h2><%= storage.storageName %></h2>
                        <p>total storage: <%= storage.totalStorage %></p>
                        <p>available storage: <%= storage.availableStorage %></p>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>
</body>
</html>